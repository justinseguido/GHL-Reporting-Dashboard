# ── GoHighLevel API v2.0 Configuration ─────────────────
# Create a Private Integration at: Settings > Integrations > Private Integrations
# Copy the API key after creating the integration
GHL_API_KEY=your_private_integration_api_key

# The Location ID (sub-account ID) to pull data from
# Found in: Settings > Business Profile > General (or from the URL)
GHL_LOCATION_ID=your_location_id

# GHL API v2 base URL (do not change unless GHL migrates)
GHL_BASE_URL=https://services.leadconnectorhq.com

# ── White-Label Branding ───────────────────────────────
# Agency display name shown in the dashboard header
AGENCY_NAME=Your Agency Name

# URL to your agency logo (displayed in header, recommended: 200x60px PNG)
AGENCY_LOGO_URL=https://yourlogo.com/logo.png

# Client business name shown alongside agency branding
CLIENT_NAME=Client Business Name
